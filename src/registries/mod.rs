//! Domain registry Docker (ghcr.io, dst.) dan tautannya ke server
//! (`server_registries`). Dipakai dari langkah 3 (opsional) wizard tambah
//! server — tidak ada halaman manajemen registry berdiri sendiri di Fase 1
//! (`docs/plan.md` "Yang sengaja tidak dikerjakan").

pub mod repo;
